spring.application.name=jmkiosk
### 기본 설정
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp
spring.mvc.static-path-pattern=/static/**
# 정적 리소스 핸들링 활성화
spring.web.resources.static-locations=classpath:/static/

spring.datasource.url=jdbc:mysql://localhost:3306/kiosk_db?serverTimezone=Asia/Seoul
spring.datasource.username=root
spring.datasource.password=12341234

# JPA 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.database=mysql

# JPA가 테이블을 만든 '후'에 data.sql이 실행되도록 순서 조정
spring.jpa.defer-datasource-initialization=true

#SQL 실행 출력
logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG
logging.level.org.springframework.jdbc.core.StatementCreatorUtils=TRACE

### 파일 업로드 설정
# 파일 한 개당 최대 용량
spring.servlet.multipart.max-file-size=10MB
# 한 번의 요청에 담긴 총 파일 용량
spring.servlet.multipart.max-request-size=10MB


#### TEST용

# 1. DB 접속 주소 (이 이름을 정확히 기억하세요: testdb)
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driverClassName=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=

# 2. H2 콘솔 설정
#spring.h2.console.enabled=true
# ★ 중요: 콘솔에서도 이 주소를 똑같이 써야만 접속됩니다.
#spring.h2.console.settings.web-allow-others=true

#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# 4. (선택) SQL 파일 자동 실행 (만약 data.sql을 쓰신다면)
#spring.sql.init.mode=always